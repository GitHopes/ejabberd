# ejabberd
## Makefile
Para instalar dependencias (tkinter, xvfb) y correr.
```bash
make
```
Solo instalar dependencias (tkinter, xvfb).
```bash
make install-deps
```
Para correr el script.
```bash
make run
```
Para cambiar de version.
```bash
make runv2
```
